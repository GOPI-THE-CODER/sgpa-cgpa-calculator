// Preload script for Electron application security.
// No APIs are exposed at the moment, but this file keeps contextIsolation enabled.

window.addEventListener('DOMContentLoaded', () => {
  // Future safe bridge logic can be added here.
});
